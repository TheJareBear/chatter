# chatter
Pretty simple but ugly web chat service.
Backend uses MySQL server on local host with credentials of:

            $host = "localhost";
            $user = "root";
            $pass = "";
            $db = "chatterbox";
